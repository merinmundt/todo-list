.text-center
  %h1 Edit Employee

  = form_with model: @employee, local: true do |form|
    .form-group
      = form.label :name, "Employee Name"
      = form.text_field :name, class: "form-control"
    
    .form-group
      = form.label :role, "Employee Role"
      = form.text_field :role, class: "form-control"
    
    .form-group.mt-3
      = form.submit "Update Employee", class: "btn btn-outline-primary"
      = link_to "Back to Employees", @employee, class: "btn btn-primary ml-2"
